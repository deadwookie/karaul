<h1>Project data</h1>
{{#each model.projects}}
  <ul>
    <li>name: {{name}}</li>
    <li>projectStage: {{projectStage}}</li>
    <li>keyCategory: {{keyCategory}}</li>
    <li>userCount: {{userCount}}</li>
    <li>budget: {{budget}}</li>
    <li>
      employees:
      <ul>
      {{#each employees}}
        <li>
          name: {{name}} | salary: {{salary}}
        </li>
      {{/each}}
      </ul>
    </li>
  </ul>
{{/each}}
